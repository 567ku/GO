# v1.3 补充规格：订单“确认双保险”（Response + UDS + Query 收敛）
> 目的：把你口头说的“双保险机制”写成**可实现、可验收**的工程规则，避免开发自由发挥。  
> 适用：Binance USD-M Futures；与 v1.3 冻结文档配套。

---

## 1) 双保险到底是什么（用大白话）
对每一次“下单/改单/撤单”，我们不只依赖一种消息来源，而是按优先级用三层信号来确认：

1) **请求回包（Response）**：WS 下单接口立即返回的结果（最快）  
2) **私域推送（UDS Event）**：用户数据流推送订单/成交状态（最可靠，用于二次确认）  
3) **查询接口（Query）**：用同一个 clientOrderId 或 orderId 主动查（兜底收敛）

核心原则：
- **本地状态只能从“事实信号”推进**，不能靠猜（比如“我发出去就当 OPEN”）。
- **clientOrderId 在状态未明确之前绝不变更**（保证幂等与可追踪）。

---

## 2) 通用状态推进规则（Engine 单写者）
### 2.1 本地 OrderSlot 的核心字段（必须实现）
- `clientOrderId`（固定）
- `orderId`（可能先空，后补）
- `state`（SUBMITTED/OPEN/FILLED/CANCELED/REJECTED/EXPIRED）
- `lastExchangeStatus`（NEW/FILLED/CANCELED… 原样保留）
- `updateAtMs`（最后一次更新来源的时间）
- `lastSource`：`RESP | UDS | QUERY`（用于排障）

### 2.2 终态优先（不可违反）
一旦进入终态（FILLED/CANCELED/REJECTED/EXPIRED），任何旧消息不得覆盖回 OPEN/SUBMITTED。

### 2.3 “回包优先，但不独裁”
- 如果 Response 已经给出终态（例如直接 FILLED）：直接落终态。
- 如果 Response 给出 NEW：可以落 OPEN（但仍允许后续 UDS/QUERY 把它推进到终态）。
- 如果 Response 超时/失败：**保持 SUBMITTED**，等待 UDS 或 Query 收敛。

---

## 3) 三类动作的“双保险”流程（可直接编码）
### 3.1 PLACE（下单）
**目标**：最终确定该 CLID 对应的订单是否存在，以及处于何种终态。

流程：
1) Engine 生成 Task(PLACE)，把 OrderSlot 置为 `SUBMITTED`，并记录 `submitAtMs` 与 `leaseExpireAtMs`。
2) Executor 发送 WS `order.place`。
3) 收到 Response：
   - 若返回 status=NEW：写入 `orderId`（如果有），state=OPEN，lastSource=RESP。
   - 若返回 status=FILLED：写入成交均价/数量，state=FILLED，lastSource=RESP。
   - 若返回 REJECTED/EXPIRED：state=REJECTED/EXPIRED，lastSource=RESP。
4) 并行地（无论是否有 Response）：UDS 可能推送订单更新或成交更新：
   - 若推送包含同 CLID：按终态优先更新 state/orderId/executedQty/avgPrice，lastSource=UDS。
5) 若 `leaseExpireAtMs` 到期仍未进入 {OPEN, FILLED, REJECTED, EXPIRED}：
   - 进入“Query 兜底”：发起 QUERY_ORDER（用 `origClientOrderId=clid`），并带 `queryAttempt`。
6) Query 返回：
   - 若查到订单：按查询结果更新 state/orderId… lastSource=QUERY。
   - 若查不到：不要立即认为不存在（交易所可能延迟）；按策略重试 Query（见 4）。
7) 超过最大 Query 次数仍不收敛：进入 `FREEZE_RECONCILE`（冻结并走对账流程）。

**验收点**：任何 PLACE 都不会出现“本地认为 OPEN，但交易所其实不存在”长期悬挂；最终一定被 Query/对账收敛。

---

### 3.2 MODIFY（改单）
**冻结规则**：MODIFY 只允许使用交易所原生 modify（不允许自动撤+挂替代）。

流程与 PLACE 类似：
- Response/UDS/Query 三层收敛
- lease 到期先 Query，仍不收敛则 FREEZE_RECONCILE
- 若 modify 被拒绝：按错误策略（RETRY / FREEZE_RECONCILE / FATAL），不得自动转为 cancel+place

**验收点**：不会出现“改价请求丢了，本地以为改了”的漂移；最终收敛到交易所真实状态。

---

### 3.3 CANCEL（撤单）
撤单的“成功”可能表现为：
- Response 表示已取消（CANCELED）
- UDS 推送取消
- Query 显示订单已不在 openOrders / status=CANCELED 或已终态

流程：
1) 发起 CANCEL，标记 OrderSlot 为 `SUBMITTED`（或保留 OPEN 但记录 cancelInFlight=true）。
2) Response：
   - 若返回 “unknown order / already canceled / already filled”：按错误策略 `IGNORE_AS_DONE` 或直接终态收敛。
   - 若返回成功：等待 UDS/Query 把它落到 CANCELED 或终态。
3) lease 到期：QueryOrder（clid/orderId）。
4) 若 Query 显示终态：落终态；若仍不明确：FREEZE_RECONCILE。

**验收点**：撤单不会卡在“本地认为撤了但其实没撤”，或“本地认为没撤但其实已终态”的灰区。

---

## 4) Query 策略（防“查太快查不到”）
为避免你提到的“瞬时不一致/查太快”，Query 采用固定退避：
- 首次 Query 延迟：`query_delay_ms = 250`（从 lease 到期或 response 超时开始算）
- 重试次数：`max_query_attempt = 3`
- 重试间隔：`250ms, 750ms, 1500ms`（可配置）

如果 3 次仍不收敛：进入 `FREEZE_RECONCILE`（冻结对账收敛）。

> 解释：我们不靠“无限查”，而是快速收敛到“冻结对账”这条稳定路径。

---

## 5) 配置字段（建议加入 RuntimeConfig）
```yaml
runtime:
  resp_timeout_ms: 2000
  uds_wait_ms: 1500
  query_delay_ms: 250
  max_query_attempt: 3
  query_backoff_ms: [250, 750, 1500]
  on_unresolved: FREEZE_RECONCILE
```

---

## 6) 与 v1.3 现有机制的关系（不冲突）
- **CLID 不变**：确保幂等与可 Query。
- **Lease 回收**：负责发现“卡住/没确认”的动作。
- **Freeze/Reconcile**：负责最终强制收敛，使本地与交易所一致。
- **UDS 推送**：负责二次确认与更快推进终态。

---

## 7) 你要的“保守、可上线”断言（开发必须遵守）
- 下单/改单/撤单：任何一个动作最终必走到**终态或冻结对账**，不能无限 SUBMITTED。
- 本地永远能解释“我为何认为它是这个状态”（lastSource + lastExchangeStatus + timestamps）。
- 任何时候都不允许因为“没看到推送”就擅自改 cycle/换 CLID。

