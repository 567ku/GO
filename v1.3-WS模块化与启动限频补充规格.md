# v1.3 补充规格：WS 模块化落地 + 启动批量布单限频（一次性写死）
> 目的：把你现在最担心的“WS 重构细节、避免臃肿、启动批量下单卡死、文档分散导致漏实现”一次性写成硬规则。
> 适用：Binance USDⓈ-M Futures（U 本位合约）。与 v1.3 冻结文档/字段规范配套。

---

## 0) 复用 vs 重写（结论：复用底座，重写编排层）
你描述“1 天掉线 1–2 次但代码臃肿”，这通常不是底层 WSConn 不稳，而是 **WS 与策略耦合过深 + 重连/超时/对账散落各处**。

v1.3 采取：
- **复用**：已经证明稳定的 WSConn 底座（dial/readPump/writePump/pongHandler/deadline/reconnect/backoff）。
- **重写**：三条 WS 的“业务编排层”（ws_manager + 三个 client 文件），把策略全部交给 Engine。

> 冻结规则：WS 模块只做通讯与事件转换，不做任何网格决策。

---

## 1) 文件与职责（固定为四文件）
你的文件划分是正确方向。v1.3 冻结如下：

### 1.1 ws_manager.go（统一总控）
职责：
1) 启动/停止三条 WS（Ticker / UserStream / OrderWS）
2) 统一健康监控（lastPongAt/lastMsgAt/errCount）
3) 统一重连与退避策略（指数退避 + 抖动）
4) listenKey 生命周期（获取、续期、失效重建）
5) **断线事件发给 Engine**，触发 Freeze/Reconcile（由 Engine 执行）
6) 对外提供统一接口：`Start(ctx) / Stop() / Health() / Stats()`

必须持有的引用：
- `engineEventCh chan<- EngineEvent`（唯一对外输出）
- `orderTaskCh <-chan Task`（Executor 或 Engine 下发的任务队列输入）
- `executorResultCh chan<- EngineEvent`（把 OrderWS 回包转为 ExecutorResultEvent 再发给 Engine）

**禁止做的事情**
- 禁止在 manager 中做“对账差异分析”“缺口扫描”“窗口移动”
- 禁止 manager 直接调用下单/撤单（只能由 Executor/Task 驱动）

### 1.2 ws_ticker.go（公共行情）
职责：
- 连接公共行情 WS
- 订阅 ticker/price（你目前的消息源保持一致即可）
- 解析成 `PriceTickEvent` 发给 Engine
- 允许 tick 合并：只保留最新价（Engine 再做越界判断）

禁止：
- 禁止下单、撤单、改状态、触发对账

### 1.3 ws_user_stream.go（私域 UDS）
职责：
- 获取 listenKey（REST），并连接 UDSWS
- 解析订单更新/成交更新为 `OrderUpdateEvent/TradeUpdateEvent` 发给 Engine
- listenKey 续期（按配置周期），失败时提前重连（不要等断线）
- 连接状态变化发送 `WSStateEvent(channel=UDS, state=...)` 给 Engine

禁止：
- “触发重连后的短对账”这种对账决策不在 UDSWS；只发事件给 Engine，由 Engine 决定进入 Reconcile。

### 1.4 ws_order.go（WS 下单/改单/撤单）
职责：
- 接收 `Task`（PLACE/MODIFY/CANCEL/QUERY）并执行：order.place/order.modify/order.cancel/query*
- 维护 `pending` 相关性： `reqId -> taskId`
- 回包转换成 `ExecutorResultEvent` 发给 Engine
- 自己也做请求超时（尽快反馈），但最终兜底由 Engine lease 处理

禁止：
- 禁止直接改 Level/OrderSlot/Profit/Cycle
- 禁止做“撤+挂”去替代 modify（除非 taskType 明确指示）

---

## 2) 三条 WS 的连接共性（必须一致，否则后面又补丁）
每条 WSConn 都必须具备：
- 单 readPump goroutine（只读）
- 单 writePump goroutine（只写；禁止并发写）
- PongHandler + ReadDeadline（可判断活性）
- 重连：指数退避 + 抖动（200ms -> 5s，成功后复位）
- 连接状态变化必须发 `WSStateEvent` 给 Engine

> 你之前 ping/pong 修复属于这一层的强制项。

---

## 3) 断线/重连与系统动作（冻结）
### 3.1 私域关键链路断线 => 立即 Freeze
触发：UDSWS 或 OrderWS 出现 DISCONNECTED。  
行为：manager 只做一件事 → 发 `WSStateEvent(DISCONNECTED)` 给 Engine。  
Engine 收到后必须：
- `engineMode=FREEZE`
- Gate 掉 PLACE/MODIFY/CANCEL（只允许 QUERY）
- 缓存 WS 事件等待对账流程

### 3.2 重连成功 => 必须走 Reconcile（不允许“直接恢复下单”）
触发：UDSWS 或 OrderWS RECONNECTED。  
Engine 必须执行：Freeze → Snapshot → Apply → ReplayWS → GapScan → Unfreeze。

> 这就是你“不要相信当时那一下，只相信对账结果”的工程化落地。

---

## 4) 启动批量布单限频（你新增的硬规则）
你遇到的问题：启动算出 200 个格子，需要下 200 笔，结果只下了 100 笔就“卡住”。这通常是触发了交易所频率/限流/连接侧拥塞。

v1.3 冻结一个 **启动批量节流器（Bootstrap Pacer）**：

### 4.1 规则（写死，可配置）
- 若单次 TaskPlan 中 `PLACE_*` 数量 `> 100`：
  - 先发前 100 个
  - **等待 20 秒**
  - 再发下一批 100 个
  - 循环直到发完
- 仅对“启动阶段/对账后大批量补单”启用（平时小规模补单无需强制 20s）

### 4.2 配置字段（加入 RuntimeConfig）
```yaml
runtime:
  bootstrap_batch_size: 100
  bootstrap_batch_cooldown_sec: 20
  bootstrap_apply_on_start: true
  bootstrap_apply_on_reconcile: true
```

### 4.3 触发点
- 程序启动第一次 GapScan 生成的 TaskPlan
- Reconcile 结束后的 GapScan 生成的 TaskPlan（可能一次性补很多）

### 4.4 工程实现方式（不破坏单写者）
- Engine 生成 TaskPlan 后，不直接一次性 push 到 Executor。
- Engine 调用一个 `PlanDispatcher`：按 batch 发送，发送之间由 Engine 定时器事件驱动（不会阻塞 Engine）。
- 发送节奏属于“执行编排”，不是“策略决策”，但仍由 Engine 统一调度，避免并发写状态。

---

## 5) “文档分散会漏实现”的解决方案（冻成交付物）
你本地已有：
- U本位合约 WebSocket API完整文档.md  
- U本位合约REST API完整文档（交易相关）.md  
- U本位合约错误代码分类及详解.md  
- key.txt  
- ws_*.go（现有代码）  
- v1.3 需求文档与补充规范

v1.3 要求仓库里新增一个 **Docs Index（唯一入口）**：  
- `/docs/INDEX.md`：列出“哪些功能查哪个文件”，并且要求 Qoder 在实现每个模块前先打开对应章节。

`/docs/INDEX.md` 至少包含：
- order.place / order.modify / order.cancel / queryOrder：对应 WebSocket API 文档章节路径
- user data streams（listenKey / keepalive / event types）：对应 UDS 文档章节路径
- 错误码：对应错误码文档章节路径
- 安全：key.txt 只用于本地加载，禁止提交仓库（gitignore）

---

## 6) WS 模块验收用例（必须通过）
1) UDSWS 人为断网 30 秒：Engine 立刻 Freeze；重连成功后必须 Reconcile 并恢复挂单
2) OrderWS 人为断网 30 秒：同上；pending 请求必须超时回灌错误
3) 启动补单 200 笔：按 100 笔 + 20 秒 + 100 笔完成，不出现“只下 100 笔就停”
4) goroutine 泄漏检查：反复掉线重连 20 次，goroutine 数量稳定（不持续增长）
5) 并发写：任何时候不触发 gorilla/websocket 并发写 panic（单 writePump）
