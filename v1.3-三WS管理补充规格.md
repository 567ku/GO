# v1.3 补充规格：三条 WebSocket 管理（重构但不返工）
> 目的：把“WS 管理细节”也冻结成工程约束，避免开发时又开始自由发挥。  
> 本规格与《v1.3 工程开发版-冻结终版》《v1.3 字段与接口规范》配套使用。

---

## 1) 一句话结论：旧代码不要“整块照搬”，只复用稳定底座
你现状是：**1 天掉线 1–2 次**，说明“总体可用”，但代码已经臃肿。  
v1.3 推荐策略：

- **复用**：底层 WS 连接组件里“已经证明稳定”的部分（dial/readPump/writePump/pingpong、JSON 解码）。
- **重写**：WS 编排层（3 条连接的生命周期、断线冻结、重连触发对账、pending 请求相关性、指标与日志）。

原因很现实：网格引擎 v1.3 的稳定性核心是 **单写者 Engine + Freeze/Reconcile**。旧 WS 代码只要和这套架构耦合过深，就会继续变肥、继续补丁化。

---

## 2) 为什么交易所看起来“毫秒级撤/补”？你怎么达到“秒级体感”
交易所的体验来自：
- 他们并不每次全量操作 1000 单；是“差异化计划 + 发送排队 + 并行 IO + 收敛”
- 执行端和策略端分离，策略只下发“计划”，执行层尽快推送请求并异步收返回

你要的“肉眼秒挂”：
- **本地计算**：GapScan & 任务生成（毫秒级）
- **网络执行**：由 Executor 以队列推进（几十毫秒到数百毫秒）
- **一致性**：靠 lease 回收 + Freeze/Reconcile 收敛，而不是等待每个请求“同步成功”

---

## 3) 三条 WS 的角色定义（冻结）
v1.3 固定使用三条连接：

### 3.1 Market WS（公共行情）
- 职责：推送最新价/标记价（写成 `PriceTickEvent` 发给 Engine）
- 允许：tick 合并（只保留最新价格）
- 禁止：在 MarketWS 内做任何交易逻辑、下单、改状态

### 3.2 User Data Stream WS（私域推送，listenKey）
- 职责：订单更新/成交更新/账户变动推送（写成 `OrderUpdateEvent/TradeUpdateEvent` 发给 Engine）
- 特性：需要 `listenKey`，并定期 keep-alive
- 禁止：在 UDSWS 内直接修正网格状态（只能发事件给 Engine）

### 3.3 Trade WS API（私域下单/改单/撤单，request/response）
- 职责：执行 `order.place / order.modify / order.cancel / query` 的 WS 请求；回包以 `ExecutorResultEvent` 形式回灌 Engine
- 特性：需要签名、请求 id 相关性（reqId -> pending map）
- 禁止：在 TradeWS 内做“对账/缺口扫描/窗口移动”一切策略运算（只能执行任务）

> 关键点：三条 WS 的共同原则是“只做通讯，不做策略”。策略只在 Engine。

---

## 4) 连接生命周期（每条 WS 都必须实现同一套状态机）
### 4.1 ConnState（建议）
- DISCONNECTED → CONNECTING → CONNECTED → (SUBSCRIBED/READY) → DISCONNECTED…

### 4.2 每条连接都必须具备的三件事
1) **读循环 readPump**：单 goroutine 读取，解析后丢到内部 channel
2) **写循环 writePump**：单 goroutine 写入，外部发送只入队（避免 gorilla/websocket 并发写崩溃）
3) **心跳与超时**：
   - 设置 `SetPongHandler` 或等价机制，收到 pong 更新 `lastPongAt`
   - 设置 `ReadDeadline`/`WriteDeadline`，超时主动断开触发重连
   - 客户端定时 ping（频率可配置），服务端 ping 也要正确响应

> 你之前“ping/pong 修复”属于 4.2 的必选项。

### 4.3 重连策略（统一实现）
- 指数退避 + 抖动：例如 200ms 起步，上限 5s（可配置）
- 连接建立成功后，退避计数清零
- 每条连接要有：`reconnectCount`, `lastDisconnectReason`, `lastConnectedAt` 指标日志

---

## 5) 断线时系统行为（需求冻结：不允许各写各的）
### 5.1 任何私域关键链路断线，立即 Freeze
触发条件：UDSWS 或 TradeWS 进入 DISCONNECTED。
动作：
- Engine 收到 `WSStateEvent(channel=UDS/TRADE, state=DISCONNECTED)` 后：
  - `engineMode = FREEZE`
  - 停止下发 PLACE/CANCEL/MODIFY（Executor gate）
  - 仅允许 QUERY 类任务（为对账做准备）

### 5.2 重连成功不等于恢复交易 —— 必须先 Reconcile
触发条件：UDSWS 或 TradeWS RECONNECTED。
动作：进入固定对账流程：Freeze → Snapshot → Apply → ReplayWS → GapScan → Unfreeze。

> 这条是你“不要相信当时那一下，只相信对账结果”的工程化落实。

---

## 6) TradeWS（WS 下单）必须具备的“请求相关性与超时”
### 6.1 请求相关性（pending map）
- 每个请求有唯一 `reqId`（uuid 或原子自增）
- `pending[reqId] = {taskId, createdAt, deadlineAt, onResult}`
- 回包带 `id=reqId` → 取出 pending → 生成 `ExecutorResultEvent` 发 Engine
- 任何回包都只做两件事：解析 + 回灌（不写策略状态）

### 6.2 请求超时与 Engine lease 的关系（不要二套超时打架）
建议：
- TradeWS 自己也要有请求超时（例如 2s），超时就回灌一个“超时错误”结果
- Engine 侧“最终裁判”仍是 lease：即使 TradeWS 没回灌，Engine 也会 lease 回收重试并最终对账收敛

> 结论：TradeWS 超时是“尽快反馈”，Engine lease 是“最终兜底”。

---

## 7) listenKey 管理（UDSWS 必做）
- 获取 listenKey（REST）
- 建立 UDSWS 连接：`wss://.../ws/<listenKey>`
- 定时 keep-alive（例如 25–30 分钟一次；具体以官方建议为准，做成配置项）
- keep-alive 失败：视为 UDSWS 将来必断 → 提前触发重连与对账（不要硬扛到断线）

---

## 8) 连接“变臃肿”的典型原因（v1.3 明确禁止）
以下逻辑严禁写进 WS 模块：
- 任何网格层的状态修改（cycle、clid、level、profit）
- 任何撤/补/改单的决策
- 任何对账差异分析
- 任何“看到某事件就立刻下单”的快捷路径

WS 模块只允许：连接管理 + 消息收发 + 将消息转换成 EngineEvent/ExecutorResultEvent。

---

## 9) 建议的代码分层（给 Qoder 的落地结构）
```
/pkg/ws/
  conn.go            # 通用 WSConn(readPump/writePump/heartbeat/reconnect)
  market_client.go   # MarketWS：订阅/解析 -> PriceTickEvent
  uds_client.go      # UDSWS：listenKey生命周期/解析 -> OrderUpdateEvent/TradeUpdateEvent
  trade_client.go    # TradeWS：请求相关性/签名/回包 -> ExecutorResultEvent
/pkg/engine/
  engine.go          # 单写者事件循环 + 状态机
  reconcile.go       # Freeze/Reconcile/Gaps
/pkg/executor/
  executor.go        # Task gate + 调用 trade_client(或 REST fallback)
```

---

## 10) 验收用例（WS 层必须通过）
1) 单条连接断网 10 秒：能自动重连，不出现 goroutine 泄漏（重连次数增长后仍稳定）  
2) UDSWS 断线：Engine 立刻 Freeze，不再下发交易动作  
3) TradeWS 断线：同上；并且 pending 请求能超时回灌错误  
4) listenKey keep-alive 失败：能提前重建 listenKey 并重连 UDSWS  
5) ping/pong：`lastPongAt` 持续更新；无 pong 能在超时后断开并重连  
6) 写入并发：任何时候不会出现 gorilla/websocket 的并发写 panic（必须单 writePump）

---

## 11) 你要的“到底复用还是重写？”落地决策
- 只要你能把旧 WS 代码改到满足：
  1) 单 writePump（无并发写）
  2) pong handler + read deadline（可判断活性）
  3) 重连退避可控（不会风暴）
  4) 断线事件能发给 Engine 触发 Freeze  
  并且把“策略逻辑”全部剥离出去  
  —— 那就可以 **复用** 旧 WS 底座。

- 如果旧代码里“WS 收到事件就直接改网格状态/直接下单”的耦合太深：
  —— 建议直接保留旧代码为 `legacy/`，按本规格重写 `/pkg/ws/*_client.go` 三个薄客户端。

