# 币安网格机器人 v1.3 —— 字段定义与接口规范（给 Qoder 直接落地）
> 目标：把“需求”落成 **唯一真源的数据结构/字段/枚举/接口契约**，避免“需求一套、代码一套”。  
> 适用版本：与《网格需求文档 v1.3（工程开发版-冻结终版）》一致。

---

## 0. 全局约定（非常重要）
### 0.1 单写者（Single Writer）
- 任何会改变**策略状态**的字段，只允许在 `Engine` 事件循环线程内写入。
- `Executor`/goroutine 只能并发“发请求”，不能直接改 `Level/Order/Profit/Cycle` 这类字段。

### 0.2 时间与数值
- 所有时间戳：`UnixMs int64`（UTC 毫秒）。
- 内部价格/数量：建议用“整数刻度”避免 float 误差：
  - `priceTicks int64`，刻度= `10^pricePrecision`
  - `qtyTicks int64`，刻度= `10^qtyPrecision`
- 对 Binance WS API：`DECIMAL`（price/quantity）应以 **JSON 字符串**发送，不要用 float。Binance 文档明确要求 DECIMAL 为字符串。

参考：Binance USD-M WS API 基本信息：DECIMAL 应为 JSON 字符串、INT 应为 JSON 整数。  
（https://developers.binance.com/docs/zh-CN/derivatives/usds-margined-futures/websocket-api-general-info）

### 0.3 clientOrderId 合规
- USD-M WS 下单的 `newClientOrderId` 必须符合正则：`^[\.A-Z\:/a-z0-9_-]{1,36}$`。  
参考：Binance USD-M WS 下单参数说明（order.place）。

---

## 1. 枚举定义（强制）
> 所有枚举值必须按下面字符串实现（日志、快照、对账都依赖一致）。

### 1.1 方向
- `GridSide`：`"LONG"` | `"SHORT"`

### 1.2 引擎模式
- `EngineMode`：`"RUNNING"` | `"FREEZE"` | `"RECONCILING"`

### 1.3 持仓模式
- `PositionMode`：`"ONE_WAY"` | `"HEDGE"`

### 1.4 订单用途
- `OrderPurpose`：`"ENTRY"` | `"TP"`

### 1.5 订单状态（本地）
- `OrderState`：
  - `"NONE"`
  - `"SUBMITTED"`（请求已发出，等待确认）
  - `"OPEN"`（交易所 NEW）
  - `"PARTIAL"`（可选：executedQty > 0 且未终态）
  - `"FILLED"`（终态）
  - `"CANCELED"`（终态）
  - `"REJECTED"`（终态）
  - `"EXPIRED"`（终态）

**终态优先不变量（必须）**  
一旦进入 `FILLED/CANCELED/REJECTED/EXPIRED`，禁止被任何旧事件覆盖回 `OPEN/SUBMITTED`。

### 1.6 Task 类型
- `TaskType`：
  - `"PLACE_ENTRY"`
  - `"PLACE_TP"`
  - `"MODIFY_ENTRY"`（仅原生 modify，不允许隐式撤+挂）
  - `"CANCEL_ORDER"`
  - `"QUERY_OPEN_ORDERS"`
  - `"QUERY_RECENT_TRADES"`
  - `"QUERY_ORDER"`
  - `"QUERY_POSITION_MODE"`

### 1.7 Task 状态
- `TaskState`：`"PENDING"` | `"INFLIGHT"` | `"DONE"` | `"FAILED"` | `"DEAD"`

---

## 2. 配置字段（Config Schema）
> 文件：`config.yaml`（或 json）。字段必须齐全；缺省值按表给定。

### 2.1 ExchangeConfig
| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---:|---:|---|
| name | string | Y | `binance_um` | 固定 USD-M Futures |
| symbol | string | Y | - | 如 BTCUSDT |
| api_mode | string | Y | `websocket` | `websocket`/`rest`（v1.3建议 websocket） |
| recv_window_ms | int | N | 5000 | Binance recvWindow |
| ws_trade_url | string | N | `wss://ws-fapi.binance.com/ws-fapi/v1` | WS 下单连接（order.place） |
| ws_market_url | string | N | `wss://fstream.binance.com/ws` | 公共行情 |
| uds_url | string | N | `wss://fstream.binance.com/ws` | 用户数据流（listenKey） |
| listen_key_keepalive_sec | int | N | 1500 | 25min（可调） |
| session_max_sec | int | N | 86000 | WS API 连接 24h 断开前重连保护 |
| ping_pong_required | bool | N | true | 必须按 Binance ping/pong 规则响应（服务器每3分钟 ping，10分钟无 pong 断开） |

> WS ping/pong 规则参考：Binance WS API 基本信息。

### 2.2 GridConfig
| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---:|---:|---|
| prefix | string | Y | - | GridPrefix，订单隔离；必须满足 newClientOrderId 合规字符集合 |
| side | GridSide | Y | - | LONG/SHORT |
| step_ticks | int64 | Y | - | 网格步长（ticks） |
| win_min_ticks | int64 | Y | - | 窗口下沿（ticks） |
| win_max_ticks | int64 | Y | - | 窗口上沿（ticks） |
| tp_keep_outside_levels | int | N | 5 | TP 扩展保留 N 格（固定 5，允许配置） |
| time_in_force | string | N | `GTC` | 基本用 GTC |
| allow_market_fill | bool | N | true | 允许限价单瞬间成交（视为FILLED即可） |
| tick_coalesce | bool | N | true | 行情可合并，只保留最新 tick |

约束（必须校验）
- step_ticks > 0
- win_min_ticks < win_max_ticks
- tp_keep_outside_levels >= 0

### 2.3 QtyConfig
| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---:|---:|---|
| mode | string | Y | `BASE` | `BASE`=按币数量；`QUOTE`=按U成本 |
| base_qty_ticks | int64 | N | - | BASE 模式的数量（ticks） |
| quote_usd_ticks | int64 | N | - | QUOTE 模式投入U（ticks），需换算 quantity |
| price_precision | int | Y | - | 价格精度（如 1 表示 0.1） |
| qty_precision | int | Y | - | 数量精度（如 3 表示 0.001） |
| min_qty_ticks | int64 | N | 0 | 交易所最小qty（可由 exchangeInfo 注入） |
| min_notional_ticks | int64 | N | 0 | 最小名义（可选） |
| round_mode | string | N | `DOWN` | 下单数量与价格的取整策略（默认向下） |

### 2.4 CompoundConfig
| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---:|---:|---|
| enabled | bool | Y | false | 是否启用复利 |
| threshold_usd_ticks | int64 | N | 0 | 触发阈值 A>=T |
| increment_base_qty_ticks | int64 | N | 0 | 每次增加的 base qty |
| max_base_qty_ticks | int64 | N | 0 | 最大 base qty |
| requote_open_entries | bool | N | false | 是否对未成交 Entry 统一 MODIFY 数量（默认关闭） |

### 2.5 RuntimeConfig
| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---:|---:|---|
| lease_place_ms | int | N | 3000 | PLACE/MODIFY 的 lease |
| lease_modify_ms | int | N | 3000 | MODIFY 的 lease |
| lease_cancel_ms | int | N | 2000 | CANCEL 的 lease |
| max_attempt | int | N | 8 | 重试上限 |
| backoff_profile | string | N | `exp_200ms_5s` | 退避策略名称 |
| reconcile_on_start | bool | N | true | 启动先对账 |
| reconcile_on_ws_reconnect | bool | N | true | WS 重连成功触发对账 |
| freeze_on_ws_disconnect | bool | N | true | WS 断线立即 Freeze |
| reconcile_time_window_sec | int | N | 3600 | recentTrades 查询时间窗（1h） |
| persist | bool | N | true | 是否落盘快照 |
| snapshot_path | string | N | `data/grid_state.json` | 状态快照路径 |
| wal_path | string | N | `data/grid_wal.log` | WAL 路径（可选） |

---

## 3. 持久化：状态快照字段（Snapshot Schema）
> 文件：`grid_state.json`（必须能完整恢复 RUNNING 所需信息）。  
> 写入采用“先写 tmp 再 rename 原子替换”（若系统限制，则允许异步写队列，但必须保证最终一致）。

### 3.1 GridStateSnapshot（顶层）
```json
{
  "version": "1.3",
  "savedAtMs": 0,
  "symbol": "BTCUSDT",
  "prefix": "GRID_A1",
  "engineMode": "RUNNING",
  "positionMode": "HEDGE",
  "side": "LONG",
  "pricePrecision": 1,
  "qtyPrecision": 3,

  "window": { "winMinTicks": 0, "winMaxTicks": 0, "stepTicks": 0, "tpKeepOutsideLevels": 5 },
  "market": { "lastPriceTicks": 0, "lastPriceAtMs": 0 },

  "qty": { "mode": "BASE", "entryQtyTicks": 0, "baseQtyTicks": 0, "quoteUsdTicks": 0 },
  "compound": { "enabled": true, "poolATicks": 0, "thresholdUsdTicks": 0, "incrementBaseQtyTicks": 0, "maxBaseQtyTicks": 0 },

  "levels": [
    {
      "levelId": 0,
      "priceTicks": 0,
      "cycle": 0,
      "entry": { ...OrderSlot... },
      "tp": { ...OrderSlot... }
    }
  ],

  "clidIndex": { "orderIdByClid": { "GRID_A1:E:12:3": 12345678 } }
}
```

### 3.2 WindowState
| 字段 | 类型 | 说明 |
|---|---|---|
| winMinTicks | int64 | 窗口下沿 |
| winMaxTicks | int64 | 窗口上沿 |
| stepTicks | int64 | 网格步长 |
| tpKeepOutsideLevels | int | TP 扩展保留格数 |

### 3.3 MarketState
| 字段 | 类型 | 说明 |
|---|---|---|
| lastPriceTicks | int64 | 最新价 |
| lastPriceAtMs | int64 | 最新价时间 |

### 3.4 QtyState
| 字段 | 类型 | 说明 |
|---|---|---|
| mode | string | BASE/QUOTE |
| entryQtyTicks | int64 | 当前生效的 Entry 数量（复利后会改变） |
| baseQtyTicks | int64 | 初始 base qty |
| quoteUsdTicks | int64 | 初始 quote usd |

### 3.5 CompoundState
| 字段 | 类型 | 说明 |
|---|---|---|
| enabled | bool | 开关 |
| poolATicks | int64 | 累计池 A（USD ticks） |
| thresholdUsdTicks | int64 | 触发阈值 |
| incrementBaseQtyTicks | int64 | 每次增加的 qty |
| maxBaseQtyTicks | int64 | 最大 qty |

### 3.6 LevelState（每个格子一行）
| 字段 | 类型 | 说明 |
|---|---|---|
| levelId | int | 连续整数或映射ID（必须稳定） |
| priceTicks | int64 | 格子价格 |
| cycle | int | 循环次数（终态闭环后递增） |
| entry | OrderSlot | Entry 单状态槽 |
| tp | OrderSlot | TP 单状态槽 |

### 3.7 OrderSlot（Entry/TP 通用）
```json
{
  "purpose": "ENTRY",
  "clientOrderId": "GRID_A1:E:12:3",
  "orderId": 1573346959,
  "state": "OPEN",
  "priceTicks": 0,
  "origQtyTicks": 0,
  "executedQtyTicks": 0,
  "avgPriceTicks": 0,
  "updateAtMs": 0,
  "lastExchangeStatus": "NEW"
}
```
字段解释：
- clientOrderId：本轮固定 ID，状态未明确前不可变
- orderId：交易所返回的 orderId（有则填）
- lastExchangeStatus：交易所 status（NEW/FILLED/CANCELED…）原样存，便于对账排错

---

## 4. 事件与接口契约（Engine <-> Executor <-> WS/REST）
### 4.1 Engine 输入事件（唯一入口）
`EngineEvent`（oneof）：
- `PriceTickEvent`
  - symbol, priceTicks, eventAtMs
- `OrderUpdateEvent`（来自用户数据流或 queryOrder/openOrders）
  - symbol, clientOrderId, orderId, status, side, positionSide, priceTicks, origQtyTicks, executedQtyTicks, avgPriceTicks, updateAtMs
- `TradeUpdateEvent`（来自 recentTrades 或成交推送）
  - symbol, clientOrderId, orderId, qtyTicks, priceTicks, tradeTimeMs, realizedPnlTicks(可选)
- `WSStateEvent`
  - channel: MARKET/UDS/TRADE, state: CONNECTED/DISCONNECTED/RECONNECTED, atMs
- `TimerEvent`
  - kind: LEASE_SCAN / RECONCILE_SCHEDULED, atMs
- `ExecutorResultEvent`
  - taskId, taskType, ok, errno(code), errmsg, rawResponse, atMs, parsedOrderUpdate(optional)

**规则**：任何事件都不得直接改状态，必须由 Engine 在 `OnEvent()` 中处理。

### 4.2 Executor 输出（Engine 只投 Task）
`Task` 字段（必须）：
| 字段 | 类型 | 说明 |
|---|---|---|
| taskId | string | ULID/UUID |
| type | TaskType | 上述枚举 |
| symbol | string | 交易对 |
| levelId | int | 关联格子（QUERY 类可为 -1） |
| purpose | OrderPurpose | ENTRY/TP（QUERY 类可空） |
| clientOrderId | string | PLACE/MODIFY 的 newClientOrderId；QUERY_ORDER 用 origClientOrderId |
| orderId | int64 | CANCEL/QUERY_ORDER 可用 |
| priceTicks | int64 | PLACE/MODIFY |
| qtyTicks | int64 | PLACE/MODIFY |
| positionSide | string | BOTH/LONG/SHORT（按 PositionMode 路由） |
| reduceOnly | *bool | One-way TP 必须 true；HEDGE 必须 nil（不发送） |
| attempt | int | 由 Engine 管理 |
| leaseExpireAtMs | int64 | 由 Engine 管理 |
| createdAtMs | int64 | - |

### 4.3 Binance WS 下单格式（关键点）
- WS API 请求 `id` 任意（uuid/自增），但同一时间不要复用。  
- 签名 payload 需将除 signature 外的参数按字母序拼接生成。  
- price/quantity 作为 DECIMAL 应以字符串传输。  

参考：Binance USD-M WS API 基本信息。

### 4.4 reduceOnly / positionSide 路由（必须与 Binance 规则一致）
- `reduceOnly`：文档明确 “Cannot be sent in Hedge Mode”。
- `positionSide`：Hedge 模式必须传 LONG/SHORT；One-way 默认 BOTH。

参考：Binance USD-M WS 下单参数表（order.place）。

---

## 5. 核心算法字段落地（避免实现偏差）
### 5.1 CLID 生成（必须一致）
- `EntryCLID = {prefix}:E:{levelId}:{cycle}`
- `TpCLID    = {prefix}:T:{levelId}:{cycle}`
- 任何时候必须保证 CLID 满足 `^[\.A-Z\:/a-z0-9_-]{1,36}$`。

### 5.2 窗口移动步数 k
- 若 `P > winMax`：`k = ceil((P - winMax)/step)`
- 若 `P < winMin`：`k = ceil((winMin - P)/step)`

**实现建议**（ticks 全为 int64）：
- `ceilDiv(a,b) = (a + b - 1)/b`（a,b>0）

### 5.3 移动后撤/留规则（字段驱动）
- Entry：`entry.priceTicks` 不在 `[winMin, winMax]` => 生成 CANCEL_ORDER
- TP：扩展窗口：
  - `extMin=winMin - keep*step`
  - `extMax=winMax + keep*step`
  - tp.priceTicks ∈ [extMin,extMax] => KEEP，否则 CANCEL_ORDER

### 5.4 GapScan（缺口扫描）输出 TaskPlan
对每个 level：计算 `DesiredState` 与 `ActualState` 的差异，生成最小任务集：
- 缺 Entry（应有但没有 OPEN/SUBMITTED）=> PLACE_ENTRY 或 MODIFY_ENTRY
- 多余 Entry（不该在窗口内/外规则不满足）=> CANCEL_ORDER
- 缺 TP（该有但无）=> PLACE_TP
- 超扩展窗口 TP => CANCEL_ORDER

**注意**：GapScan 既在“窗口移动完成后”执行，也在“对账回放 WS 后”执行。

### 5.5 Reconcile（Freeze -> Snapshot -> Apply -> ReplayWS -> GapScan -> Unfreeze）
- Freeze：gate 掉 PLACE/CANCEL/MODIFY（只允许 QUERY）
- Snapshot：openOrders/recentTrades/positionMode（均按 prefix 过滤）
- Apply：以快照校正 `OrderSlot` 状态（终态优先）
- ReplayWS：按到达顺序回放缓存的 WS 事件
- GapScan：生成补齐/撤销计划
- Unfreeze：执行计划后进入 RUNNING

---

## 6. 错误策略表（外置文件 Schema）
> 文件：`binance_error_policy.yaml`，主逻辑只认识 class，不把错误码写死进代码。

```yaml
version: "1.3"
rules:
  - code: -2011
    class: IGNORE_AS_DONE
    note: "Unknown order sent -> cancel 视为达成"
  - code: -1003
    class: RETRY
    backoff: exp_200ms_5s
    note: "rate limit"
  - code: -4061
    class: FATAL
    note: "positionSide 与持仓模式不匹配"
  - code: -1021
    class: RETRY
    backoff: exp_200ms_5s
    note: "timestamp for this request is outside of the recvWindow"
defaults:
  on_network_error: RETRY
  on_timeout: RETRY
  on_unknown: FREEZE_RECONCILE
```

`class` 可选值：`RETRY | FREEZE_RECONCILE | FATAL | IGNORE_AS_DONE`

---

## 7. 给 Qoder 的硬性实现 Checklist（照做不跑偏）
1) 代码里必须定义上述所有枚举字符串，并用于日志/快照/对账过滤。  
2) Engine 单写者：Level/Order/Cycle/Profit 只能在 Engine 事件循环改。  
3) Executor 并发没问题，但结果必须以 `ExecutorResultEvent` 回灌 Engine。  
4) 所有 price/qty 内部用 ticks，发 WS 时转 DECIMAL 字符串。  
5) `newClientOrderId` 强制校验正则与长度上限。  
6) Hedge 模式禁止发送 reduceOnly；One-way TP 必发 reduceOnly=true。  
7) “窗口移动后立即 GapScan”，别等下一次 tick。  
8) Reconcile 严格走 Freeze->Snapshot->Apply->ReplayWS->GapScan->Unfreeze，不允许跳步。  
