# 币安网格机器人需求文档 v1.3（工程开发版）
> 本文用于直接交付开发。目标：需求冻结、实现可验收、避免“边做边改”。

## 1. Scope / 非 Scope
### 1.1 Scope（v1.3 必须实现）
- 交易所：Binance USDⓈ-M Futures（U 本位合约）
- 交易行为：网格 + 窗口上移/下移
- 订单归属隔离：仅管理 `clientOrderId` 以前缀 `GridPrefix` 开头的订单
- 复利：按“网格循环闭环”利润累计触发，提高后续 Entry 数量
- 重启/重连：冻结对账（快照校正 + WS 回放 + 缺口扫描）后恢复运行
- 幂等重试：任务 lease 超时回收，依赖 clientOrderId 去重

### 1.2 非 Scope（v1.3 明确不做）
- 多交易所、多币种、多网格并行（可留接口，但不实现）
- 自动恢复人工撤 TP
- 错误码细粒度策略写进主逻辑（改为外置 policy 文件）

---

## 2. 术语与业务规则
## 2.0 运行架构与并发模型（需求冻结：避免实现跑偏）
### 2.0.1 三个角色
- **事实输入（Facts）**：WS/REST 只负责把“事实事件”投递进引擎（价格、订单更新、成交、断线/重连）。
- **总指挥/引擎（Engine，单线程）**：**本地状态的任何修改只能在一个事件循环内完成**（避免多线程写状态导致对不齐）。
- **执行器（Executor）**：只负责执行引擎下发的 Task（下单/改单/撤单/查询），把回包结果再投递回引擎。

### 2.0.2 强制约束
- Executor 可以并发“发请求”，但 **落本地状态必须串行（由 Engine 单点处理）**。
- WS 事件、REST 回包、定时器（lease 回收/对账触发）全部进入 Engine 的事件队列统一处理。
- 行情（ticker）允许合并：只保留最新价格，但越界检测必须及时触发（见 tick_coalesce 配置）。

---

## 2.1 并发与“锁”策略（需求冻结：100% 防并发错乱）
> 说明：我们要的不是“到处加 Mutex”，而是 **通过架构保证没有共享写入**，从源头上做到 100% 不乱。  
> 结论：v1.3 采用 **单写者（Single Writer / Actor）** 模式——引擎是唯一写本地状态的地方，因此大部分“锁需求”会自然消失。

### 2.1.1 单写者硬约束（必须遵守）
以下本地状态 **只能在 Engine 事件循环内修改**（严禁任何 worker/goroutine 直接写）：
- level 状态、orderRef、cycle、CLID 生成与递增
- 复利累计池 A、entryQty 调整
- Freeze/Unfreeze 开关、Reconcile 阶段切换
- Task 状态（PENDING/INFLIGHT/DONE/FAILED/DEAD）与 attempt/lease 字段

> 这条落地后：**不需要“改状态锁 / 利润锁 / ID锁 / level锁”**，因为没有并发写入发生。  
> “锁”由事件队列的串行处理天然提供。

### 2.1.2 v1.3 仍然必须实现的 3 类“并发控制”（不是 Mutex，但等价于锁）
1) **Level-Serial（同 level 串行令牌）**  
   - 表达形式：Engine 内的 `inflightToken`（布尔/计数），而不是 Mutex。  
   - 规则：同一 level 同时最多 1 个 INFLIGHT 任务；新任务要么合并要么排队。

2) **Lease 超时回收（卡住自救）**  
   - 表达形式：任务的 `leaseExpireAt` + Engine 定时扫描回收。  
   - 规则：超时 => 回滚到 PENDING 重试（带 backoff）；不依赖“defer unlock”。  
   - 说明：`defer Unlock()` 只对“正常返回或 panic 被 recover”有效；**对网络卡住/阻塞无效**。

3) **Freeze 栅栏（Reconcile/断线期间禁止写操作）**  
   - 表达形式：Engine 的 `mode=FREEZE/RECONCILING/RUNNING` 状态机 + Executor 的 gate。  
   - 规则：FREEZE 期间仅允许 QUERY 类任务出队；PLACE/CANCEL/MODIFY 一律拒绝/缓发。

### 2.1.3 仅 Executor 侧需要的最小锁（如果实现为并发请求）
如果 Executor 使用多 goroutine 并行发请求，下列共享结构必须加锁或使用并发安全结构：
- `pendingRequests`（reqId -> ctx/callback）：需要 Mutex
- `reqId` 生成：使用 atomic 自增或 Mutex
- RateLimiter：使用 channel token bucket 或并发安全实现

> 注意：这些锁仅限 Executor 内部，不允许触碰 Engine 的策略状态。

### 2.1.4 明确禁止的实现（避免以后又开始“补丁加状态”）
- 禁止多个 worker/goroutine 直接读写 level/order/cycle/profit 等策略状态（哪怕你加了锁也禁止）  
  原因：一旦走共享状态 + 多锁路线，需求会迅速膨胀、对账会变复杂、bug 会变隐蔽。
- 禁止用“撤单+重挂”伪装成 modify（除非策略明确允许，见 8.4）

---

### 2.1 Grid / Window / Level
- `gridStep`：网格步长（价格间隔）
- `level`：一个价格格子（离散点）
- `window`：机器人**只管理** `[winMin, winMax]` 区间内的网格；窗口可整体移动

### 2.2 Order Types（业务定义）
- Entry：开仓单（LONG=BUY LIMIT；SHORT=SELL LIMIT）
- TP：平仓单（LONG=SELL LIMIT；SHORT=BUY LIMIT）

### 2.3 Prefix isolation（硬约束）
系统只对满足 `clientOrderId startsWith(GridPrefix)` 的订单进行：
- reconcile/openOrders/trades/queryOrder
- cancel/modify
其余订单一律忽略，禁止误操作。

---

## 3. 配置（建议 YAML）
```yaml
exchange:
  name: binance_um
  symbol: BTCUSDT
  api_mode: websocket   # websocket|rest（v1.3 建议 websocket，但允许封装层切换）
  recv_window_ms: 5000

grid:
  prefix: "GRID_A1"          # GridPrefix：订单隔离的唯一标识
  side: "LONG"               # LONG|SHORT（v1.3 固定运行中不可切换）
  step: 300                  # gridStep
  win_min: 100000            # 初始窗口下沿
  win_max: 109000            # 初始窗口上沿
  tp_keep_outside_levels: 5  # TP 窗口外保留格数（固定为 5，可配置）

qty:
  mode: "BASE"               # BASE=按币数量, QUOTE=按U成本（按U需要计算quantity），v1.3建议先BASE
  base_qty: 0.001            # BASE 模式下每次 Entry 的币数量
  quote_usd: 100             # QUOTE 模式下每次 Entry 投入U（若启用）
  precision_qty: 3
  precision_price: 1

compound:
  enabled: true
  threshold_usd: 100         # CompoundThreshold
  increment_base_qty: 0.0001 # CompoundIncrement（BASE 模式）
  max_base_qty: 0.01         # CompoundMaxQty
  requote_open_entries: false # 是否把未成交 Entry 统一改数量（默认 false）

runtime:
  lease_place_ms: 3000
  lease_modify_ms: 3000
  lease_cancel_ms: 2000
  max_attempt: 8
  tick_coalesce: true        # 允许只保留最新价格事件
  reconcile_on_start: true
```

---

## 4. 持仓模式与下单参数路由（冻结规则）
### 4.1 启动/重连时查询持仓模式
- 调用 `GET /fapi/v1/positionSide/dual` 获取 `dualSidePosition`（true=Hedge，false=One-way）。

### 4.2 参数路由（必须按此实现）
**One-way（dualSidePosition=false）**
- Entry：`reduceOnly=false`（或不传）
- TP：`reduceOnly=true`
- `positionSide`：默认 BOTH（可显式传 BOTH）

**Hedge（dualSidePosition=true）**
- 禁止发送 reduceOnly（交易所不接受）
- 必须发送 `positionSide=LONG/SHORT`
  - LONG：Entry=BUY+LONG；TP=SELL+LONG
  - SHORT：Entry=SELL+SHORT；TP=BUY+SHORT

---

## 5. clientOrderId 规范（幂等 + 可对账）
### 5.1 格式
- `EntryCLID = {prefix}:E:{levelId}:{cycle}`
- `TpCLID    = {prefix}:T:{levelId}:{cycle}`

### 5.2 cycle 递增规则（避免重复/乱序）
- cycle 仅在 **TP 达到终态且该 level 本轮闭环完成** 后递增。
- 任何请求未回包、WS 未确认、状态不明确时 **禁止变更 CLID**。

---

## 6. 状态机与不变量（必须写成不可违反的约束）
### 6.1 本地 OrderState（最小集合）
- NONE
- SUBMITTED（已发送请求，等待确认）
- OPEN
- PARTIAL（可选，v1.3可以仅记录 executedQty 不单独建状态）
- FILLED（终态）
- CANCELED（终态）
- REJECTED/EXPIRED（终态）

### 6.2 三条硬不变量
1) **Terminal-first**：一旦进入 FILLED/CANCELED/REJECTED/EXPIRED，禁止被任何旧信息覆盖回 OPEN/SUBMITTED。  
2) **SUBMITTED≠OPEN**：place/modify 回包必须解析 status；只有明确 NEW 才可视为 OPEN；若直接 FILLED 则直接终态。  
3) **Level-serial**：同一 level 同时最多一个 INFLIGHT 任务；新任务必须合并或排队，禁止乱序下发。

---

## 7. 窗口移动（核心业务逻辑，按此实现）
### 7.1 触发与步数
最新价 P：
- 若 `P > winMax`：上移 `k = ceil((P - winMax)/step)`
- 若 `P < winMin`：下移 `k = ceil((winMin - P)/step)`
一次性移动 k 格。

### 7.2 移动后撤/留规则
- **Entry**：价格不在 `[winMin, winMax]` 一律 CANCEL
- **TP**：保留扩展窗口范围内：
  - `extMin = winMin - keep*step`
  - `extMax = winMax + keep*step`
  - TP price ∈ [extMin, extMax] => KEEP，否则 CANCEL

### 7.3 缺口扫描（窗口移动后 / 回到区间内都必须执行）
- **窗口移动完成后：立即执行一次缺口扫描**（因为按 k 的计算，当前价 P 应已落入新窗口）
- 当价格回到 `[winMin, winMax]`：也必须执行一次缺口扫描

缺口扫描生成 TaskPlan：
- 缺 Entry：place/modify
- 缺 TP：place
- 不该有的 Entry：cancel
- 超出扩展窗口的 TP：cancel

> v1.3 不引入“只维护市价上下 N 格”逻辑，订单数量控制由窗口移动策略实现。

---

## 8. 任务系统（Executor + Lease 超时回收）
### 8.1 Task 类型
- PLACE_ENTRY / PLACE_TP
- MODIFY_ENTRY（v1.3 建议 Entry 用 modify 减少撤挂，可按适配层能力决定）
- CANCEL_ORDER
- QUERY_OPEN_ORDERS / QUERY_RECENT_TRADES / QUERY_ORDER（reconcile 用）

### 8.2 Lease 回收
- Task 出队 -> INFLIGHT -> 记录 `takenAt`、`leaseExpireAt`
- `now > leaseExpireAt`：`attempt++`，回滚为 PENDING 重新入队（带 backoff）
- 超过 `max_attempt`：标记 DEAD + 告警；状态最终由 reconcile 收敛

### 8.3 幂等保证
- 下单/改单以 `origClientOrderId` 或 `orderId` 标识唯一目标
- 重试同一任务时 CLID 不变（直到终态明确）
- cancel 对“不存在/已终态”的返回按“目标达成”处理（避免卡死）

---

### 8.4 改单（MODIFY）实现约束（按你的冻结口径）
- v1.3 的“改单”指 **交易所原生 modify**（不是隐式撤单再挂单）。
- 若 modify 被交易所拒绝/不支持当前订单状态：不要自动改成“撤+挂”；应按失败分支进入重试/或触发 FREEZE_RECONCILE 收敛处理（见错误策略分类）。

---
## 9. Reconcile（重启/重连对账）——固定三段式
### 9.1 触发
- 启动
- **私域 WS 断线：立即 Freeze（停止 place/cancel/modify，下发只允许 query）**
- 私域 WS 重连成功：进入对账流程并收敛状态
- 异常阈值
- 定时（可选）

### 9.2 流程（必须原子化）
1) `Freeze`：暂停 place/cancel/modify 下发（Executor 只允许 query）
2) `Snapshot`：拉取
   - openOrders（仅 prefix）
   - recentTrades（仅 prefix 或时间窗过滤后再匹配）
   - position mode（dualSidePosition）与可选 positions
3) `ApplySnapshot`：以快照校正本地订单/level 状态
4) `ReplayWS`：回放冻结期缓存的 WS 事件（按到达顺序）
5) `GapScan`：基于最新本地状态生成 TaskPlan（补缺口/撤多余）
6) `Unfreeze`：执行 TaskPlan，恢复 Running

---

## 10. 复利（闭环利润记账）
### 10.1 记账口径
仅在 level 完成一轮闭环时记账：
- LONG：Entry FILLED + TP FILLED
- SHORT：Entry FILLED + TP FILLED

利润：
- LONG：`(tpAvgPrice - entryAvgPrice) * qty`
- SHORT：`(entryAvgPrice - tpAvgPrice) * qty`

### 10.2 触发与更新
维护累计池 `A`：
- `A += profit`
- while `A >= threshold`:
  - `entryQty = min(entryQty + increment, maxQty)`
  - `A -= threshold`

更新生效范围：
- 默认仅影响“后续新挂 Entry”
- `requote_open_entries=true` 时：对未成交 Entry 统一 MODIFY 数量（注意交易所 modify 需要同时给 price+quantity，且可能被拒绝）

---

## 11. 错误处理（策略表外置）
主逻辑只实现：
- RETRY（带 backoff）
- FREEZE_RECONCILE（进入对账）
- FATAL（停机告警）
- IGNORE_AS_DONE（例如 cancel 返回“已不存在”）

具体错误码 -> 动作映射写在 `binance_error_policy.yaml`：
```yaml
- code: -4061
  class: FATAL
  note: "positionSide 与当前持仓模式不匹配"
- code: -2011
  class: IGNORE_AS_DONE
  note: "cancel 目标不存在，视为已达成"
- code: -1003
  class: RETRY
  backoff: "exp_200ms_5s"
  note: "rate limit"
```

---

## 12. 日志与验收
### 12.1 必须日志
- Task：taskId、levelId、type、clid、attempt、lease、latency、result
- Order：clid <-> orderId 映射、状态转移原因（WS/REST/Modify/Cancel）
- Reconcile：freeze时长、快照数量、回放数量、gap任务数量

### 12.2 验收用例（必须通过）
1) 同一 level 不出现两张同时 OPEN 的前缀订单  
2) 注入“请求卡住 10s”：系统能 lease 回收继续跑（不死锁、不丢格）  
3) 私域 WS 断线 30s 再重连：执行冻结对账后可收敛恢复挂单  
4) 手工单不被撤/不被改（前缀隔离生效）  
5) 窗口越界：区间外 Entry 全撤；TP 仅保留扩展 5 格范围  
