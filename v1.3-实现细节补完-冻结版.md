# v1.3 实现细节补完（冻结版，用于消除歧义）
> 本文是对 v1.3 文档包的“实现层补完”，解决：初始化窗口/level 生成、数量规则、事件字段、对账冲突矩阵、WS 缓存、退避策略、实盘安全前置检查等细节歧义。
> 不改变 v1.3 总体方向，只把“默认值/公式/边界处理”写死，便于直接交付 Qoder。

---

## A. 网格核心参数：初始化窗口与 Level 生成（写死）
### A1. step 的定义
- v1.3 的 `step` 为 **固定网格步长**（ticks/价格刻度），不做 ATR/波动率动态步长。
- 动态 step 属于 v1.4+ 非 Scope。

### A2. 初始窗口 winMin/winMax 的来源
v1.3 冻结两种方式，但默认只支持 **手工输入**：
1) **MANUAL（默认）**：配置文件直接给定 `winMinTicks/winMaxTicks/stepTicks`，引擎不做“自动算窗口”。
2) **AUTO_CENTER（可选开关，默认关闭）**：
   - 仅用于辅助生成 MANUAL 值（建议 CLI 工具），不建议在实盘自动变更窗口。
   - 公式：
     - `winMin = anchorPrice - levelsBelow * step`
     - `winMax = anchorPrice + levelsAbove * step`
   - anchorPrice：启动时读取的 `lastPrice`（ticks）并做对齐（见 A3）。

> 结论：你要“需求稳定”，就用 MANUAL。AUTO_CENTER 只做工具，不进 v1.3 主逻辑。

### A3. 价格对齐（避免 level 漂移）
为了保证“同一价格永远落在同一个 levelId”，v1.3 强制对齐：
- 校验：`winMinTicks % stepTicks == 0` 且 `winMaxTicks % stepTicks == 0`
- 若不满足：启动直接报错退出（避免“自动对齐后窗口变了”引发隐性偏差）。

### A4. Level 列表生成规则（写死）
- 全量 levels：从 `winMinTicks` 起，按 `stepTicks` 递增直到 `<= winMaxTicks`。
- `levelId`：从 0 开始递增（稳定且可重建）。

伪代码：
```go
levels := []Level{}
for price := winMin; price <= winMax; price += step {
  id := len(levels)
  levels = append(levels, Level{levelId:id, priceTicks:price, cycle:0})
}
```

### A5. 启动后“应该挂哪些 Entry”（写死）
v1.3 为单向网格逻辑（每个 level 只有 Entry→TP 闭环），启动时不依赖“市价上下 N 格”，只依赖窗口与当前价 P：

- LONG：只在 **当前价以下（含等于）** 的 level 生成 ENTRY BUY（因为买单必须在市价下方排队）  
  `desiredEntry(levelPrice) = (levelPrice <= P)`
- SHORT：只在 **当前价以上（含等于）** 的 level 生成 ENTRY SELL  
  `desiredEntry(levelPrice) = (levelPrice >= P)`

> 这样启动不会在市价上方挂 LONG 买单（无意义），也不会在市价下方挂 SHORT 卖单。

### A6. TP 价格规则（写死）
- LONG：`tpPrice = entryPrice + step`
- SHORT：`tpPrice = entryPrice - step`
- 仅当 Entry 达到终态 FILLED（本轮有仓位）才允许创建 TP。  
- Entry PARTIAL 不创建 TP（v1.3 简化口径）。

---

## B. 订单数量（qty）与复利：公式与口径（写死）
### B1. 下单 qty 的两种模式（与字段规范一致）
1) BASE：配置 `entryQtyTicks` 直接下单数量（推荐，最稳定）  
2) QUOTE：配置 `quoteUsdTicks`，每次下单按当次下单价换算：
   - `qtyTicks = floor( quoteUsdTicks / priceTicks )`（在 ticks 体系内按比例换算）
   - 再按 `qtyPrecision` 向下取整
   - 若 `< minQty`：该任务直接标记 DEAD（并打日志），不进入交易所

### B2. 复利的增量规则（写死，不做比例复利）
- v1.3 复利使用 **固定增量**：
  - 当 `poolA >= threshold`：
    - `entryQty = min(entryQty + increment, maxEntryQty)`
    - `poolA -= threshold`（可循环多次扣减）
- 复利“增量”与“利润金额/币价”不挂钩（否则实现会分叉）。

### B3. poolA（利润累计）口径（写死）
- 仅在 **一个 level 完成闭环**时累计：Entry FILLED + TP FILLED（同 cycle 同 level）。
- 利润计算（忽略手续费的默认口径，最简单也最稳定）：
  - LONG：`profit = (tpAvgPrice - entryAvgPrice) * filledQty`
  - SHORT：`profit = (entryAvgPrice - tpAvgPrice) * filledQty`
- 若能拿到真实手续费字段（可选）：可在 profit 上减去手续费；但 v1.3 默认先不引入，避免字段缺失导致分歧。

---

## C. WS -> Engine 交互字段：ExecutorResultEvent 与缓存（写死）
### C1. ExecutorResultEvent 字段（必须具备）
> 目标：Engine 能完整解释“这个任务发生了什么”，且可对账/可排障。

必填字段：
- `taskId`（string）
- `taskType`（TaskType）
- `ok`（bool）
- `reqId`（string：WS API 的 request id）
- `atMs`（int64：本地收到回包时间）
- `errorCode`（int：Binance code；ok=true 时为 0）
- `errorMsg`（string）
- `raw`（string/bytes：原始回包，可截断保存）

可选字段（强烈建议实现）：
- `exchangeTimeMs`（交易所时间，如回包有）
- `parsedOrder`（结构体）：
  - `clientOrderId`
  - `orderId`
  - `status`（NEW/FILLED/CANCELED…）
  - `priceTicks`
  - `origQtyTicks`
  - `executedQtyTicks`
  - `avgPriceTicks`

### C2. WS 事件缓存（只在 FREEZE/RECONCILING）
- 仅缓存 UDS 事件与必要的 ExecutorResultEvent（为了对账后 replay）。
- 缓存上限（写死，防 OOM）：
  - `maxEvents = 10_000`
  - `maxAgeMs = 10*60*1000`（10 分钟）
- 触发溢出：
  - 设置 `replayOverflow=true`
  - 丢弃最旧事件（ring buffer）
  - 对账阶段直接走“快照强收敛 + GapScan”，不依赖完整回放（保证安全优先）。

---

## D. Reconcile 快照精度与冲突矩阵（写死）
### D1. recentTrades 时间窗（避免漏匹配）
v1.3 采用“滑动窗 + 回退覆盖”的固定规则：
- `tEnd = nowMs`
- `tStart = max(nowMs - reconcile_time_window_sec*1000, lastReconcileAtMs - 5*60*1000)`
  - lastReconcileAtMs 初始为 now-window（第一次启动）
  - “回退 5 分钟”用于覆盖延迟推送与乱序

### D2. 冲突矩阵（Apply 阶段的硬规则）
对同一订单（以 CLID 为主键），按以下优先级写死：

1) **终态优先**：任何来源只要给出终态（FILLED/CANCELED/REJECTED/EXPIRED），本地立刻进入终态，不允许回退。
2) **快照优先于本地非终态**：
   - 本地 SUBMITTED，但快照 OPEN → 更新为 OPEN（补齐 orderId/status）
   - 本地 OPEN，但快照无此订单：进入 `UNKNOWN` 分支，必须发 QUERY_ORDER（用 CLID）；不得擅自判定 canceled
3) **事件时间戳优先**（若提供 updateTime）：较新的覆盖较旧的（在不违反终态优先前提下）。

### D3. UNKNOWN 分支的收敛（写死）
- 在 reconcile 过程中若出现 UNKNOWN：
  - 立即计划 `QUERY_ORDER`（最多 3 次，按双保险的固定退避）
  - 仍无法确定：保持 FREEZE，触发 `FREEZE_RECONCILE`（即继续对账，不进入 RUNNING）

---

## E. 重试与退避（Backoff）公式（写死）
### E1. exp_200ms_5s 的计算
- 基础：`base=200ms`
- 指数：`delay = min(base * 2^(attempt-1), 5s)`
- 抖动（jitter）：`delay = delay * rand(0.8, 1.2)`
- attempt 从 1 开始；attempt>max_attempt → 任务进入 DEAD，并触发 FREEZE_RECONCILE。

### E2. 告警（可插拔，但给出必须触发点）
v1.3 不强制实现邮件/短信，但必须预留 `AlertSink` 接口，并在以下场景调用：
- 连续 FREEZE 超过 N 分钟（默认 3 分钟）
- Reconcile 失败或反复失败超过 M 次（默认 3 次）
- 任务进入 DEAD（特别是 PLACE/MODIFY/CANCEL）
- clientOrderId 无法收敛（UNKNOWN 超过阈值）

---

## F. 实盘安全约束（Preflight 与运行时校验，写死）
### F1. 启动 Preflight（必须全部通过，否则拒绝启动 RUNNING）
- 读取并校验 config（step、winMin/winMax 对齐、prefix 合规）
- 查询并确认 positionMode（ONE_WAY/HEDGE）
- 拉取 exchangeInfo（tickSize/stepSize/minQty/minNotional/precision）并与 config 一致（不一致则报错）
- 校验 `entryQtyTicks` 或 QUOTE 换算后 qty >= minQty
- 校验窗口内 level 数量不超过硬上限（默认 2000，防配置事故）

### F2. 前缀隔离的硬规则（任何接口都必须过滤）
- openOrders/recentTrades/UDS 事件：只处理 `clientOrderId` 以 prefix 开头的记录。
- 对非前缀订单：一律忽略，不写入 snapshot，不纳入对账差异。
- 若交易所返回某条记录没有 clientOrderId（极少）：忽略并记一条 warn。

---

## G. 与现有文档的合并建议（一次性收口）
把本文作为 v1.3 文档包的“附录 F（实现细节补完）”，并在 Qoder 开发安排里新增一条强制步骤：
- 开发开始前：先实现本文 A~F 的校验与 schema，写入单元测试，避免后续分歧。

