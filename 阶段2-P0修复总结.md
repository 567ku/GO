# 阶段 2 - P0 必改清单修复总结

**修复日期**: 2025-11-26  
**状态**: ✅ **P0 修复全部完成**

---

## ✅ P0-1: 补充 jitter（防止共振）

### 问题描述
原实现使用纯指数退避 `backoff *= 2`，缺少 jitter 会造成三条 WS 同步重连，遇到网络抖动时容易共振。

### v1.3 冻结公式
```
delay = min(base * 2^(attempt-1), max) * rand(0.8, 1.2)
```

### 修复实现

**新增辅助函数** ([types.go:159-180](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\types.go#L159-L180)):
```go
// calculateBackoffWithJitter 计算带 jitter 的退避时间
// 公式: delay = min(base * 2^(attempt-1), max) * rand(0.8, 1.2)
// 防止多条 WS 同时重连造成共振
func calculateBackoffWithJitter(base, max time.Duration, attempt int) time.Duration {
    if attempt <= 0 {
        attempt = 1
    }

    // 指数退避: base * 2^(attempt-1)
    backoff := base
    for i := 1; i < attempt; i++ {
        backoff *= 2
        if backoff > max {
            backoff = max
            break
        }
    }

    // 添加 jitter: rand(0.8, 1.2)
    jitter := 0.8 + rand.Float64()*0.4 // 0.8 ~ 1.2
    return time.Duration(float64(backoff) * jitter)
}
```

**修改三个客户端的 connectionLoop**:

1. **Market WS** ([market.go:98-151](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\market.go#L98-L151))
2. **UDS WS** ([uds.go:99-171](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\uds.go#L99-L171))
3. **Trade WS** ([trade.go:128-189](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\trade.go#L128-L189))

**修改要点**:
```go
// 之前：
backoff := c.config.ReconnectInitDelay
retries := 0
// ...
backoff *= 2
if backoff > c.config.ReconnectMaxBackoff {
    backoff = c.config.ReconnectMaxBackoff
}

// 现在：
attempt := 0
// ...
attempt++
backoff := calculateBackoffWithJitter(c.config.ReconnectInitDelay, c.config.ReconnectMaxBackoff, attempt)
```

### 退避序列示例

**无 jitter（旧版）**:
```
1s → 2s → 4s → 8s → 16s → 32s → 60s → 60s → ...
```

**有 jitter（新版）**:
```
0.8s~1.2s → 1.6s~2.4s → 3.2s~4.8s → 6.4s~9.6s → 12.8s~19.2s → 25.6s~38.4s → 48s~72s → ...
```

### 防共振效果

假设三条 WS 同时断线：
- **无 jitter**: 三条同时在 1s/2s/4s/8s... 重连 → **网络风暴**
- **有 jitter**: 重连时间错开 ±20% → **负载分散**

---

## ✅ P0-2: PongHandler 同时做两件事

### 问题描述
原实现的 PongHandler 只更新 `lastPongAt`，没有立即 `SetReadDeadline`，仍会误判断线。

### 正确实现（必须同时做）

1. **更新 lastPongAt**
2. **立刻 SetReadDeadline(now + pongTimeout)**

### 修复实现

**修改三个客户端的 connect() 方法**:

1. **Market WS** ([market.go:176-186](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\market.go#L176-L186))
2. **UDS WS** ([uds.go:224-234](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\uds.go#L224-L234))
3. **Trade WS** ([trade.go:215-225](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\trade.go#L215-L225))

**修改前**:
```go
conn.SetPongHandler(func(string) error {
    c.connMu.Lock()
    c.lastPongAt = time.Now()
    c.connMu.Unlock()
    return nil
})
```

**修改后**:
```go
conn.SetPongHandler(func(string) error {
    c.connMu.Lock()
    c.lastPongAt = time.Now()
    c.connMu.Unlock()

    // 立即设置读取超时（防止误判断线）
    if c.config.PongWait > 0 {
        conn.SetReadDeadline(time.Now().Add(c.config.PongWait))
    }
    return nil
})
```

### 为什么必须同时做？

#### 场景分析

**只更新 lastPongAt（旧版）**:
```
T0: 发送 Ping
T1: 收到 Pong → 更新 lastPongAt
T2: readPump 继续读取，但 ReadDeadline 未更新
T3: ReadDeadline 超时 → 误判断线（即使刚收到 Pong）
```

**同时 SetReadDeadline（新版）**:
```
T0: 发送 Ping
T1: 收到 Pong → 更新 lastPongAt + SetReadDeadline(T1 + PongWait)
T2: readPump 继续读取，ReadDeadline 已延长
T3: 正常等待下次消息（不会误判）
```

#### 关键时序

```
时间轴:  0s -------- 10s -------- 20s -------- 30s
         |           |            |            |
Ping:    发送        
Pong:                收到         
Old:                 更新时间     (ReadDeadline=20s超时) ✗ 误判断线
New:                 更新时间+Deadline=30s              ✓ 正常
```

---

## ✅ 附加修复: 移除最大重连次数限制

### 问题描述
原实现有 `ReconnectMaxRetries` 限制，达到上限后停止重连。这不符合实盘要求。

### v1.3 要求
- **不要做"最大重连次数限制"**
- 替代方案：达到阈值则触发 AlertSink + Engine 维持 FREEZE，**并继续重连**

### 修复实现

**删除配置字段** ([types.go:38](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\types.go#L38)):
```go
// 之前：
ReconnectMaxRetries int  // 最大重连次数（0表示无限重连）

// 现在：（已删除该字段）
// 无最大重连次数限制，持续重连
```

**删除重连检查逻辑**:
```go
// 之前：
if c.config.ReconnectMaxRetries > 0 && retries >= c.config.ReconnectMaxRetries {
    return
}

// 现在：（已删除该检查）
// 无限重连，由 Engine 决定是否 Freeze
```

### 实盘策略

1. **WS 层**: 无限重连（带 jitter）
2. **Engine 层**（待实现）: 
   - 收到 DISCONNECTED 事件 → 进入 FREEZE
   - 重连次数超过阈值（如 10 次）→ 触发告警
   - 持续保持 FREEZE 状态，等待人工介入或网络恢复

---

## 📊 修复前后对比

| 项目 | 修复前 | 修复后 | 影响 |
|------|--------|--------|------|
| **jitter** | ❌ 无 | ✅ ±20% | 防止共振 |
| **PongHandler** | ⚠️ 只更新时间 | ✅ 更新时间+Deadline | 防止误判断线 |
| **最大重连次数** | ⚠️ 可配置上限 | ✅ 无限重连 | 符合实盘要求 |

---

## 🧪 验证结果

### 编译测试
```bash
go build ./pkg/ws/...
```
✅ **通过**（无错误）

### 预期效果

1. **jitter 防共振**
   - 三条 WS 重连时间错开 ±20%
   - 网络抖动时负载分散

2. **PongHandler 双动作**
   - 收到 Pong 立即延长 ReadDeadline
   - 不会因超时误判断线

3. **无限重连**
   - WS 层持续重连（不放弃）
   - Engine 层决定 Freeze 状态
   - 告警机制触发人工介入

---

## 📝 修改文件清单

| 文件 | 修改内容 | 行数变化 |
|------|----------|----------|
| [pkg/ws/types.go](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\types.go) | 新增 calculateBackoffWithJitter<br>删除 ReconnectMaxRetries | +24, -2 |
| [pkg/ws/market.go](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\market.go) | 使用 jitter + PongHandler 双动作<br>删除最大重连检查 | +11, -14 |
| [pkg/ws/uds.go](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\uds.go) | 使用 jitter + PongHandler 双动作<br>删除最大重连检查 | +53, -58 |
| [pkg/ws/trade.go](file://c:\Users\Administrator\Desktop\GO2.0\pkg\ws\trade.go) | 使用 jitter + PongHandler 双动作<br>删除最大重连检查 | +22, -25 |

**总计**: +110 行, -99 行（净增 11 行）

---

## ✅ P0 修复完成确认

- [x] **P0-1**: jitter 补充完成（防止共振）
- [x] **P0-2**: PongHandler 双动作完成（防止误判断线）
- [x] **附加**: 移除最大重连次数限制（符合实盘要求）
- [x] **验证**: 编译通过，无错误

---

## 🚀 准备进入阶段 3

**P0 修复全部完成**，可以进入阶段 3（Engine 底座）。

**下一步任务**:
1. Engine 单写者事件循环
2. Freeze/Reconcile 状态机
3. 接收 WS 事件并更新本地状态
4. Task 生成与下发
5. Lease 超时回收
6. GapScan 缺口扫描
